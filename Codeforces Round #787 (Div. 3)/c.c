#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
	int t;
	long long int ar[30];

	scanf("%d", &t);

	while (t--) {
		int c;
		while ((c=getchar()) != '\n') {
			
		}
	}
	return (0);
}
